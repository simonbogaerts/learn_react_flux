import React from "react";

/* React.Fragment won't render anything, but allows to eturn with encompassing element. */
/* <></> =  <React.Fragment></React.Fragment>*/

const AboutPage = () => (
  <>
    <h2>About</h2>
    <p>This app uses React.</p>
  </>
);

export default AboutPage;
